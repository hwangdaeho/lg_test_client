export default function ({ app, store, route, from, redirect }) {

  // if (route.name === 'auth') {
  //   const authRoute = store.state.isExpertMode ? store.getters.userInfo : '/auth'
  //   return redirect(authRoute)
  // }
  // if (route.name === 'index') {
  //   return redirect('/user/list')
  // }
  // const user = store.state.token ? store.getters.userInfo : null
  // if (!user) {
  //   return redirect('/auth/login')
  // }
}
